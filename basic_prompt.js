/**
 * Converts a string to camelCase format.
 * 
 * The function takes a string and transforms it into camelCase, where:
 * - The first word is lowercase
 * - Each subsequent word has its first letter capitalized
 * - All spaces and special characters are removed
 * 
 * @param {string} str - The input string to convert
 * @returns {string} The camelCase version of the input string
 * 
 * @example
 * toCamelCase("hello world") // returns "helloWorld"
 * toCamelCase("convert this string") // returns "convertThisString"
 * toCamelCase("snake_case_text") // returns "snakeCaseText"
 */
function toCamelCase(str) {
    // Implementation here
}